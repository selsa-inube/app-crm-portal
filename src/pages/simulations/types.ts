export interface IDataProspect {
  name: string;
  choiceDestination: string;
  customer: string;
  value: string;
}

export interface IDataHeader {
  name: string;
  status: string;
  image?: string;
  publicCode?: string;
}
